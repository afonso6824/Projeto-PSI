import { BeginDateDirective } from './Validators/begin-date.directive';

describe('BeginDateDirective', () => {
  it('should create an instance', () => {
    const directive = new BeginDateDirective();
    expect(directive).toBeTruthy();
  });
});
