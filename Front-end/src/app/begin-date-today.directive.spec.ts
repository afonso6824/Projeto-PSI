import { BeginDateTodayDirective } from './Validators/begin-date-today.directive';

describe('BeginDateTodayDirective', () => {
  it('should create an instance', () => {
    const directive = new BeginDateTodayDirective();
    expect(directive).toBeTruthy();
  });
});
